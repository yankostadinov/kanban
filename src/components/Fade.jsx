import React from 'react';
import PropTypes from 'prop-types';
import './Fade.css';

const Fade = () => <div className="fade" />;

Fade.displayName = 'Fade';
Fade.propTypes = { children: PropTypes.node };

export default Fade;
